/**
 * Shared suggestions data — committed to repo so it appears on all browsers/devices.
 * To add a suggestion permanently, add it here and redeploy.
 * Local additions (via the UI) are saved in localStorage until committed here.
 */
const suggestionsData = [
  // Example:
  // { id: '1719900000000', name: 'Markus', text: 'Add a performance attribution section', createdAt: '2026-07-01T10:00:00.000Z' },
];

export default suggestionsData;
