/**
 * Shared vacation planner data — committed to repo so it appears on all browsers/devices.
 * To add a person permanently, add them here and redeploy.
 * Local additions (via the UI) are saved in localStorage until committed here.
 */
const vacationData = [
  // Example:
  // { name: 'Markus', weeks: [27, 28, 29, 30] },
];

export default vacationData;
